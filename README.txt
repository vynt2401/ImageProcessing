Dạ em sử dụng Python, file docFileAnh.py để đọc ảnh png là ảnh lena.png để chuyển sang raw.dat có trong folder.

Dạ em có file requirements.txt chứa các thư viện cần thiết khi triển khai một enviroment của python trong bài làm của em.

Trước hết, thầy tạo một enviroment cho python.

Sau đó thầy gõ: pip install -r requirements.txt để tải các thư viện về ạ.

Thầy chạy file docFileAnh.py dưới môi trường thầy cài ạ.

Thầy deactivate môi trường python sau đó sử dụng gcc để chạy file .c

Sau khi ra file raw.dat, thầy sử dụng file convert.c để lấy dữ liệu từ file raw.dat rồi chuyển sang file gray.dat và bin.dat ạ


